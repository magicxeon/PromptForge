# PromptForge Attribute Specification

This starter attribute library is derived from the uploaded reference list, normalized into grouped attributes suitable for building a prompt generator. It is intended as a starting point and can be expanded with additional options and model-specific mappings.
