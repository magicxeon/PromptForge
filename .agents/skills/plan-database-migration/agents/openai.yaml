interface:
  display_name: "Database Migration Planner"
  short_description: "Plan safe schema and storage cutovers"
  default_prompt: "Use $plan-database-migration to assess schema readiness and plan a reversible database migration."
policy:
  allow_implicit_invocation: true
